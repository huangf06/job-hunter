System Review Prompt                                                                                                                                                                                                                                                                                       
                                                                                                                                                                                                                                                                                                               # Job Hunter v2.0 — 全面系统审查                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        
  你是一名资深软件架构师，请对这个自动化求职系统进行全面的代码审查和架构评估。
  系统流水线：Scrape → Hard Filter → Rule Score → AI Analysis → Resume Render → Track

  请逐一审查以下维度，每个维度给出：
  - 🔴 严重问题（影响正确性/数据丢失）
  - 🟡 中等问题（影响可维护性/可靠性）
  - 🟢 改进建议（优化方向）

  ---

  ## 1. 数据完整性与数据库设计

  重点审查：
  - `src/db/job_db.py` 的所有 SQL 操作，特别是 upsert 逻辑（`insert_job` 用 ON CONFLICT）
  - `_get_conn()` context manager 的连接管理——是否有连接泄漏风险？
  - Turso embedded replica 同步模式——本地写入和云同步之间是否存在数据丢失窗口？
  - `ai_scores` 表（实际存规则评分）vs `job_analysis` 表（存 AI 分析）——命名混淆是否导致了查询错误？
  - `save_analysis` 写入 sentinel 记录 (`tailored_resume='{}'`) 防止重试——这些 sentinel 是否会被错误地当作有效分析？
  - 时间戳一致性：`save_analysis` 用 UTC，其他地方是否也统一？

  ## 2. AI 分析器可靠性 (`src/ai_analyzer.py`)

  重点审查：
  - Bullet-by-ID 系统：`_build_bullet_id_lookup()` 和 `_resolve_bullet_ids()` 的边界情况
    - 如果 `bullet_library.yaml` 中有重复 ID 会怎样？
    - ID 解析失败时的 hard reject 路径是否完备？
  - Bio Builder：`_assemble_bio()` 的所有验证分支
    - `domain_claims`, `closer_options`, `allowed_titles` 的白名单验证是否有遗漏？
    - 向后兼容（旧版字符串 bio）的处理是否可靠？
  - Token 预算控制：`get_daily_token_usage` 是否准确？跨时区是否有 off-by-one？
  - 错误恢复：API 调用失败、JSON 解析失败、响应截断时的行为
  - Prompt 注入风险：JD 内容直接拼入 prompt，是否有安全隐患？

  ## 3. 简历验证与渲染 (`src/resume_validator.py`, `src/resume_renderer.py`)

  重点审查：
  - 验证门控的完备性：
    - Title validation (BLOCKING)
    - Skill category whitelist (10 categories, BLOCKING)
    - Excluded skills check
    - 最少 2 experiences, 3 skill categories
  - 渲染器的 Jinja2 模板安全性（`autoescape=True` 是否足够？）
  - Playwright PDF 生成的稳定性——超时、内存泄漏、并发安全
  - 输出文件命名冲突处理（同公司同职位多次生成）

  ## 4. 爬虫系统健壮性

  审查所有爬虫：
  - `scripts/linkedin_scraper_v6.py`：Cookie 过期检测、反爬对策、CDP 连接断开处理
  - `src/scrapers/greenhouse.py` + `lever.py`：API rate limiting、分页边界、空响应处理
  - `src/scrapers/iamexpat.py`：Playwright 页面加载（用 domcontentloaded 而非 networkidle）、选择器变更风险
  - `scripts/multi_scraper.py`：编排逻辑、部分失败处理、重复去重
  - 所有爬虫的数据清洗：HTML 标签残留、编码问题、超长描述截断

  ## 5. 筛选与评分系统

  审查：
  - `config/base/filters.yaml` 硬规则：是否有误杀优质职位的风险？
    - `regex` 规则的 exception 只检查 title（不检查 full_text）——这个设计决策是否合理？
  - `config/base/scoring.yaml` 评分权重：是否存在某些维度被过度/不足加权？
  - 筛选和评分的可观测性——能否轻松 debug 为什么某个职位被过滤/低分？

  ## 6. 本地工作流 (`--prepare` / `--finalize`)

  审查：
  - `checklist_server.py`：HTTP server 安全性（绑定地址、CORS、输入验证）
  - `state.json` 的原子性——并发读写、崩溃恢复
  - Repost 检测逻辑：`find_applied_duplicates` 的 SQL 正确性
  - `--finalize` 的幂等性——多次运行是否安全？
  - Cover Letter 生成 + 渲染流程的错误处理

  ## 7. 面试调度系统

  审查：
  - `src/google_calendar.py`：Token 原子读写（与 MCP 共享 token 文件）
    - `_save_tokens()` 的 temp+rename 策略在 Windows 上是否可靠？
    - Token 过期检测（`expiry_date` 用毫秒）
  - `src/interview_scheduler.py`：三维评分模型的实现是否与设计一致？
  - 时区处理：用户在荷兰（CET/CEST），API 返回 UTC——转换是否正确？

  ## 8. CI/CD 与部署

  审查 `.github/workflows/job-pipeline.yml`：
  - Secret 管理（Turso token, API keys）
  - 失败重试策略
  - Turso 云 DB 在 CI 中的连接稳定性
  - 通知系统（Telegram）的可靠性——`notify.py` 的 funnel 统计是否准确？

  ## 9. 配置管理与安全

  审查：
  - 敏感信息是否被 `.gitignore` 完全覆盖？（cookies, tokens, .env, db files）
  - `config/` 目录下的 YAML 配置是否有验证？加载时是否处理了格式错误？
  - `bullet_library.yaml` 作为核心资产——是否有备份/版本控制策略？
  - 依赖版本锁定情况

  ## 10. 代码质量与可维护性

  审查：
  - 模块边界清晰度：`scripts/` vs `src/` 的职责分离是否严格？
  - 错误处理一致性：是否有 bare except、吞掉异常、日志缺失？
  - 类型注解覆盖率和一致性
  - 重复代码（特别是 DB 查询、配置加载）
  - 日志系统：是否有足够的可观测性来 debug 生产问题？

  ## 11. 已知问题的根因分析

  以下是已知但未完全解决的问题，请深入分析根因：
  - **rejection 追踪缺口**：208 个 applied 状态的职位中，很多实际已被拒。114 个职位零邮件追踪。
    根因与 mail_processor（独立项目）的 `lookback_days: 2` + `unread_only=True` + `mark_as_read=True` 配置有关。
    请评估：在 job-hunter 侧是否有可以做的补救措施？
  - **Windows 上 Turso embedded replica 的栈溢出 bug**：当前的 workaround（禁用 Turso 回退本地 SQLite）是否有更好的方案？

  ---

  ## 输出格式

  请按以上 11 个维度逐一输出，每个维度包含：
  1. 审查发现（按严重程度排序）
  2. 具体代码位置（file:line）
  3. 建议修复方案
  4. 优先级评估（P0 立即修复 / P1 本周 / P2 下次迭代）

  最后给出一个总结：
  - Top 5 最紧急问题
  - Top 5 最有价值的改进
  - 整体架构健康度评分 (1-10)