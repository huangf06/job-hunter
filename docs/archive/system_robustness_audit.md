# System Robustness Audit Prompt

## 目标

对 job-hunter v2.0 系统进行全链路功能审查，从职位爬取到简历生成，确保每个模块逻辑正确、边界条件安全、模块间数据流畅通。

## 完整 Pipeline

```
LinkedIn/Playwright 爬取
       ↓
  JSON 落盘 (data/inbox/)
       ↓
  导入 DB (jobs 表)
       ↓
  硬规则筛选 (filters.yaml → filter_results 表)
       ↓
  规则评分 (scoring.yaml → ai_scores 表)
       ↓ rule_score >= 阈值
  AI 分析 (Claude Opus → job_analysis 表)
       ↓ ai_score >= 阈值
  简历渲染 (Jinja2 + Playwright → PDF)
       ↓
  申请追踪 (applications 表)
```

## 审查清单

按 pipeline 顺序逐模块审查：

---

### 1. 职位爬取 (`scripts/linkedin_scraper_v6.py`, `scripts/playwright_scraper.py`)
- [ ] LinkedIn cookie 过期 / 无效时，是否有明确错误提示而不是静默失败？
- [ ] `--cdp` 连接 Chrome (端口 9222) 失败时的错误处理
- [ ] 爬取到的 JSON 字段是否与下游 DB import 期望的 schema 完全一致？
- [ ] 翻页 / 加载更多的逻辑是否健壮（网络慢、页面结构变化）？
- [ ] 去重逻辑：同一职位重复爬取时，是否正确跳过或更新？
- [ ] `--save-to-db` 模式 vs JSON 落盘模式，数据格式是否一致？
- [ ] 爬虫中断（Ctrl+C / 网络断开）时，已爬取的数据是否安全保存？

### 2. JD 解析 (`scripts/job_parser.py`)
- [ ] 解析逻辑是否能处理各种 JD 格式（纯文本、HTML、Markdown 混合）？
- [ ] 字段提取（title、company、location、description）在缺失时是否有默认值？
- [ ] 超长 JD（>10K 字符）是否被截断或安全处理？

### 3. 数据库层 (`src/db/job_db.py`)
- [ ] `_get_conn()` context manager 是否正确关闭连接？异常时是否 rollback？
- [ ] 所有 SQL 是否使用参数化查询（防注入）？
- [ ] 表结构（`jobs`, `filter_results`, `ai_scores`, `job_analysis`, `resumes`, `applications`）是否与代码中的字段名完全一致？
- [ ] `get_funnel_stats()` 等统计查询是否正确使用 `job_analysis`（不是 `ai_scores`）获取 AI 结果？
- [ ] 导入阶段：JSON → `jobs` 表的字段映射是否完整？缺失字段是否有默认值？
- [ ] 并发安全：多个脚本同时访问 DB 是否安全（WAL mode？锁机制？）

### 4. 硬规则筛选 (`config/base/filters.yaml` + pipeline 筛选逻辑)
- [ ] 筛选规则是否从 YAML 动态加载（不是硬编码）？
- [ ] 规则匹配是否大小写不敏感？
- [ ] 筛选结果是否正确写入 `filter_results` 表？
- [ ] 被拒绝的职位是否记录拒绝原因（方便 debug）？
- [ ] 已筛选过的职位重复运行时，是否跳过（幂等性）？

### 5. 规则评分 (`config/base/scoring.yaml` + pipeline 评分逻辑)
- [ ] 评分规则是否从 YAML 动态加载？
- [ ] 评分结果是否正确写入 `ai_scores` 表？
- [ ] 分数计算逻辑：各维度权重是否与配置一致？
- [ ] 已评分的职位重复运行时，是否跳过或更新（幂等性）？
- [ ] 边界条件：JD 为空 / 极短时，评分是否安全处理？

### 6. Pipeline 入口 (`scripts/job_pipeline.py`)
- [ ] `--process` 流程是否按正确顺序执行 import → filter → score？
- [ ] `--ai-analyze` 是否正确读取 `rule_score >= threshold` 的职位？
- [ ] `--generate` 是否正确读取 `ai_score >= threshold` 的职位？
- [ ] `--analyze-job JOB_ID` 单个职位分析是否正常工作？
- [ ] `--limit` 和 `--min-score` 参数是否正确传递到各阶段？
- [ ] `--stats`, `--ready`, `--mark-applied` 等查询命令是否正确？
- [ ] 错误处理：单个职位处理失败是否跳过并继续（不 crash 整个 batch）？
- [ ] 各阶段之间的数据交接：是否通过 DB 查询衔接（不是内存传递）？

### 7. AI 分析器 (`src/ai_analyzer.py`)

#### 7a. Bullet-by-ID 系统
- [ ] `_build_bullet_id_lookup()` 是否正确解析 `bullet_library.yaml` 中 **所有** work_experience 和 projects 下的 bullets？
- [ ] 是否遍历了 `prompt_settings.experience_keys` 和 `project_keys` 配置的所有 key？
- [ ] Prompt 中是否以 `[id] text` 格式展示所有可用 bullets？
- [ ] `_resolve_bullet_ids()` 是否正确处理：
  - 正常 ID → 查找成功，返回 verified text
  - 未知 ID → 硬拒绝（tailored_resume = `{}`）
  - 空列表 → 硬拒绝
  - 向后兼容：精确文本匹配（旧分析结果）仍可接受
- [ ] 每个 experience/project 的 bullet 数量是否有合理上下限校验？

#### 7b. Bio Builder 系统
- [ ] `_assemble_bio()` 是否正确处理：
  - 有效 role_title + domain_claims + closer_id → 组装成功
  - 无效 role_title → 硬拒绝
  - 无效 domain_claim key → 硬拒绝
  - 无效 closer_id → 硬拒绝
  - closer_id = null → 不加 closer（合法）
  - years 超过 max_years_claim → 自动 cap 到配置值
- [ ] 向后兼容：DB 中旧的字符串 bio 是否仍能正常渲染？

#### 7c. Validation Gates
- [ ] Title validation: 是否检查 `bio_builder.allowed_titles`？无效 title 是否阻断？
- [ ] Skill category validation: 是否检查 `allowed_skill_categories`？发明的类别名是否阻断？
- [ ] Excluded skills: 是否检查 `skill_tiers.excluded`？出现排除技能是否阻断？
- [ ] 最低经历数（<2）/ 最低技能类别数（<3）是否阻断？
- [ ] 所有阻断条件是否可通过 `config/ai_config.yaml` 配置？
- [ ] 任何 gate 失败时，是否正确标记为拒绝并记录原因？

#### 7d. API 调用健壮性
- [ ] Claude API 超时 / 网络错误 → 是否 retry？retry 策略（次数、间隔）是否合理？
- [ ] API 返回非 JSON（纯文本、markdown 包裹的 JSON）→ 是否有清洗逻辑？
- [ ] API 返回部分 JSON（截断）→ 是否安全降级？
- [ ] Token 预算控制：是否统计 token 消耗并在超预算时停止？
- [ ] Rate limiting (429) 是否处理？

### 8. 简历渲染器 (`src/resume_renderer.py`)
- [ ] `tailored_resume = {}` 时是否跳过渲染（不生成空 PDF）？
- [ ] Jinja2 模板渲染：`autoescape=True` 是否启用？
- [ ] 模板变量缺失时是否安全处理（不 crash）？
- [ ] Bio 渲染：是否同时支持字符串 bio（旧格式）和结构化 bio spec（新格式）？
- [ ] Bullet 内容是否从 `bullet_library.yaml` 查找（不是直接使用 AI 输出的文本）？
- [ ] Playwright PDF 生成：Chromium 未安装时是否有明确错误提示？
- [ ] 输出文件命名：是否包含公司名/职位 ID 以区分不同简历？
- [ ] 渲染结果是否正确写入 `resumes` 表？

### 9. 简历验证器 (`src/resume_validator.py`)
- [ ] 验证逻辑是否与 `bullet_library.yaml` v3.0 的当前结构一致？
- [ ] 是否验证所有 blocking gates（title、category、excluded skills、min experiences、min skill cats）？
- [ ] 验证失败时的错误信息是否清晰、可操作（指出具体哪个 gate 失败）？
- [ ] 验证器是在渲染前还是渲染后运行？顺序是否正确？

### 10. 配置一致性（横切关注点）
- [ ] `config/ai_config.yaml` 中的阈值是否与代码中的默认值一致（无硬编码覆盖）？
- [ ] `bullet_library.yaml` 的结构（字段名、嵌套层级）是否与 `ai_analyzer.py` 的解析逻辑完全匹配？
- [ ] `config/base/filters.yaml` 和 `scoring.yaml` 是否与 pipeline 筛选/评分代码一致？
- [ ] `config/search_profiles.yaml` 中的 profile 是否与爬虫的 `--profile` 参数对应？
- [ ] 所有模块是否统一使用 `from src.xxx import YYY` 导入模式？
- [ ] 所有脚本是否有 `sys.path.insert(0, PROJECT_ROOT)` 且只出现一次？

### 11. 模板系统 (`templates/base_template.html`)
- [ ] 模板中的变量名是否与 `resume_renderer.py` 传入的 context 完全一致？
- [ ] 动态内容（bullets、skills、courses）的循环逻辑是否正确？
- [ ] CSS 样式是否内联（PDF 渲染不支持外部样式表）？
- [ ] 打印/PDF 布局：是否有分页控制（避免内容被截断）？

---

## 审查方法

1. **追踪数据流**：模拟一个职位从爬取到生成 PDF 的完整生命周期，逐步追踪数据在各模块间的流转
2. **读代码**：逐文件阅读，标注每个 TODO/FIXME/HACK
3. **边界测试**：对每个函数思考 edge cases（空输入、None、超长字符串、畸形 JSON、网络失败）
4. **配置对齐**：将 YAML 配置与代码中的硬编码值交叉比对
5. **幂等性验证**：每个阶段重复运行是否安全（不重复处理、不覆盖数据）

## 输出格式

对每个发现，按以下格式报告：

```
### [严重程度] 文件:行号 — 简述
- **现状**：描述当前行为
- **风险**：可能造成什么问题
- **建议修复**：具体代码修改建议
```

严重程度：
- 🔴 **BUG**：当前逻辑错误，会导致错误结果或 crash
- 🟡 **FRAGILE**：当前能工作，但特定条件下会失败
- 🟢 **IMPROVEMENT**：功能正确但可以更健壮

## 优先级

先报告所有 🔴 BUG，再报告 🟡 FRAGILE，最后 🟢 IMPROVEMENT。
每个严重程度内，按 pipeline 顺序排列（上游问题优先于下游）。
